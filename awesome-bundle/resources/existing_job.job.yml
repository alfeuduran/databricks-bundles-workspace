resources:
  jobs:
    existing_job:
      name: existing_job
      tasks:
        - task_key: oldtask
          notebook_task:
            notebook_path: ../HelloNotebookOldJob.ipynb
            source: WORKSPACE
      queue:
        enabled: true
      performance_target: STANDARD
