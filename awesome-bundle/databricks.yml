# Databricks Asset Bundle configuration, more info here: https://docs.databricks.com/en/dev-tools/bundles/settings.html
bundle:
  name: "awesome-bundle"

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-8876629969026717.17.azuredatabricks.net

  # The 'prod' target, used for production deployment.
  prod:
    mode: production
    workspace:
      host: https://adb-3457621802558823.3.azuredatabricks.net/
      # We always use /Users/alfeu.duran@databricks.com for all resources to make sure we only have a single copy.
      root_path: /Users/alfeu.duran@databricks.com/.bundle/${bundle.name}/${bundle.target}
    
